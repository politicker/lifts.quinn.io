version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/db/query.sql"
    schema: "internal/db/schema.sql"
    database:
      uri: "postgres://localhost:5432/quinn-lifts?sslmode=disable"
    gen:
      go:
        package: "db"
        out: "internal/db"

